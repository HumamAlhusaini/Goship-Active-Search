// navigation
package components

import "github.com/HumamAlhusaini/Goship-Active-Search/internal/model"

templ SocialsLink(socialLink model.Anchor) {
	<a href={ templ.SafeURL(socialLink.Href) }>
		@socialLink.Icon
	</a>
}
